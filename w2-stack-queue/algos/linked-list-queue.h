#pragma once

#include "node.h"

class LinkedListQueue {
    private:
        Node* first;
        Node* last;
    public:
        LinkedListQueue() {}

        ~LinkedListQueue() {}

        bool isEmpty() {}
    
        void enqueue(string text) {}

        string dequeue() {}
};